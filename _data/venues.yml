# Venue abbreviations used by the `abbr` field in _bibliography/papers.bib.
# An entry here turns the badge into a link (`url`) and/or changes its color (`color`); see docs/CUSTOMIZE.md.
# Without an entry the badge is shown as plain text.
# TODO: add the ISI venue URL if you want the badge to link out, e.g.
# "ISI":
#   url: https://...
